node_modules/
out/
dist/
release/
*.log
.DS_Store
keep/*.exe
keep/*.exe~
# SQLite databases — generated on first launch, unique per server, never committed
*.db
*.db-*
# media blobs the Keep stores on disk next to its db (runtime data)
media/
# TypeScript incremental build info
*.tsbuildinfo
# local editor / agent config (settings.local.json etc.)
.claude/
# Tauri installer build artifacts (icons/ + source are kept; target/gen are generated)
installer/src-tauri/target/
installer/src-tauri/gen/
# the built installer exe (distribute via release/hand-off, not the repo)
installation_File/*.exe
# local P2P-transport test sandbox (built binaries + runtime data + scripts)
Experimental/
